{
  "name": "luna-ui-lib-pablo-demo",
  "version": "1.0.1",
  "description": "",
  "main": "dist/luna-ui-lib.umd.js",
  "module": "dist/luna-ui-lib.mjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "publish:examples": "git add . && (git commit -m \"Publish examples\" || echo \"No changes to commit\") && (git checkout gh-pages || git checkout -b gh-pages) && git rm -r * --ignore-unmatch && xcopy docs\\* . /E /Y /I && xcopy dist\\* . /E /Y /I && ren example.html index.html && git add . && (git commit -m \"Publish examples\" || echo \"No changes to commit\") && git push -f origin gh-pages",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ui",
    "components",
    "web-components",
    "luna"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "tslib": "^2.8.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  },
  "dependencies": {
    "@picocss/pico": "^2.1.1"
  }
}
